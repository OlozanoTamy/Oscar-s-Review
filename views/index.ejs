<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Oscar's Reviews</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <%- include("partials/header.ejs") %>

        <div class="container-sm">
            <div class="row">
                <% for(let libro of listOfBooks){%>
                    <div class="card col-sm-8 col-md-6 col-xxl-4 mt-5">
                        <img src="/images/cien años de soledad.jpg" class="card-img-top" alt="cover">
                        <div class="card-body">
                            <h5 class="card-title"><%= libro.title %></h5>
                            <p class="isbn">ISBN: <%= libro.isbn %></p>
                            <p class="date">Fecha de lectura: <%=libro.fecha%> </p>
                            <p class="reccomend">Recommendation:  <%= libro.recomend %>  </p>
                            <p class="resume"><%= libro.resume %></p>
                            <form action="edit" method="get">
                                <button type="submit">Editar</button>
                            </form>
                            <form action="/delete" method="post">
                                <button type="submit" onchange="this.form.submit()" name="deleteItemId" value="<%= libro.id %>">Delete</button>
                              </form>
                        </div>

                    </div> 
                <% } %>
            </div>
        </div>         
</body>
</html>







